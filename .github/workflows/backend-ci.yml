name: Backend CI

on:
  workflow_dispatch:  # disabled — enable when backend/ exists

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Ruff lint
        run: ruff check .

      - name: Mypy type check
        run: mypy app/

      - name: Run tests
        run: pytest tests/ -v --tb=short
        env:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/ember_test
          COGNITO_USER_POOL_ID: test
          COGNITO_CLIENT_ID: test
          AWS_REGION: eu-west-1
          MEM0_API_KEY: test
          ANTHROPIC_API_KEY: test

      - name: Check doc-code sync
        run: python scripts/check_doc_code_sync.py

      - name: Validate OpenAPI specs
        run: python scripts/validate_openapi.py
        env:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/ember_test
          COGNITO_USER_POOL_ID: test
          COGNITO_CLIENT_ID: test
          AWS_REGION: eu-west-1
          MEM0_API_KEY: test
          ANTHROPIC_API_KEY: test

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ember_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
